//El objeto Math

let resultado;

resultado = Math.PI; //Pi
resultado = Math.round(2.3); //Redondeo
resultado = Math.ceil(2.1); //Redondeo hacia arriba
resultado = Math.floor(2.9);  //Redondeo hacia abajo
resultado = Math.sqrt(144); //Raiz cuadrada
resultado = Math.abs(-200); //Cambiar a positivo
resultado = Math.min(3, 5, 7, 10, 25, 30); //Valor menor
resultado = Math.max(3, 5, 7, 10, 25, 30); //Valor mayor
resultado = Math.random();
resultado = Math.floor( Math.random() * 30);

console.log(resultado);
